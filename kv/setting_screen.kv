<SettingScreen>
    MDBoxLayout:
        orientation: 'vertical'
        adaptive_height: True
        #adaptive_width: True
        pos_hint: {'top': .6, 'center_x': .5}
        spacing: dp(50)
        MDRaisedButton:
            pos_hint: {'center_x': .5}
            text: 'Quit'
            text_color: 1, 1, 1, 1
            on_release: quit()
